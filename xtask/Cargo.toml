[package]
name = "xtask"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false

[dependencies]
anyhow = "1.0.100"
clap = { version = "4.5.50", features = ["derive"] }
git_changelog = { path = "../git_changelog" }

[lints]
workspace = true
